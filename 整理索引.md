# 放置修仙 项目文件夹整理索引

> 整理时间：2026-04-21
> 整理人：AI 助手
> 目的：为后续接手的 AI 或人工开发者提供清晰的目录结构说明

---

## 整理原则

1. **单文件版优先**：`放置修仙_单文件版.html` 是最终交付物，保留在根目录，方便直接打开
2. **按类型归档**：源码、文档、各版本 HTML 分别归入对应子文件夹
3. **保留原有结构**：`历史版本/`、`pets/` 已是合理子文件夹，不做改动

---

## 目录结构

```
/Users/oxohuang/WorkBuddy/20260419170725/
│
├── 放置修仙_单文件版.html      ← 最终单文件版（根目录，直接可运行）
│
├── 各版本归档/                  ← 各阶段版本 HTML（多文件版）
│   ├── 放置修仙_视觉复刻版.html
│   ├── 放置修仙_水墨优化版.html
│   ├── 放置修仙_完整版_测试.html
│   └── 放置修仙_Q版手绘风格_demo.html
│
├── 源码/                       ← 开发源码文件
│   ├── index.html
│   ├── style.css
│   ├── game.js
│   ├── test.html
│   ├── xianxia-test.html
│   └── avatar.png
│
├── 文档/                       ← 设计思路与改进文档
│   └── 放置修仙_UI优化改进思路.md
│
├── 历史版本/                   ←（保留原样，应为过往迭代存档）
│
└── pets/                       ←（保留原样，应为宠物/角色图片资源）
    └── *.png（13 张）
```

---

## 各文件夹说明

### 根目录
- **`放置修仙_单文件版.html`**：唯一保留在根目录的文件，是整合后的单文件可运行版本，直接双击即可在浏览器中打开。

### 各版本归档/
存放开发过程中产生的中间版本 HTML 文件，每个文件可能对应不同的视觉风格或功能阶段，供回溯参考。

### 源码/
存放未打包的多文件源码，包括：
- `index.html` — 主入口
- `style.css` — 样式表
- `game.js` — 游戏逻辑
- `test.html` / `xianxia-test.html` — 测试页面
- `avatar.png` — 头像资源

### 文档/
存放项目相关的设计文档、改进思路等非代码资料。

### 历史版本/ 与 pets/
这两个文件夹在整理前已存在且结构合理，未做改动。如需进一步整理，请查看各自内容后再决定。

---

## 后续建议

- 如果 `源码/` 中的文件不再需要，可以考虑归档或删除
- `历史版本/` 建议定期清理，只保留重要里程碑版本
- 新增文件时，请按类型放入对应子文件夹，保持根目录整洁
